import { FC } from "react";

const Header: FC = () => (
  <header className="w-full h-16 bg-gray-800 text-white flex items-center px-4 shadow-md">
      <h1 className="text-xl font-semibold">Affirmator</h1>
    </header>
)

export default Header;
